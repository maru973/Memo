11/13 Javascript　入門　１
            <script></script> : HTMLなどにJavascriptを埋め込める。
            <script src="script.js" defer></script>　: script.jsファイルを取り込める。
    　　　Javascript 入門　３
            DOM : Webページをコンピュータが理解しやすい形に変換するもの。

            git stash : コミットしたくない変更を退避する。　
                        作業ブランチで変更をしてたけど、他にやることができた。→　このままコミットすると中途半端。→ git stashを使用。
            .gitignore : バージョン管理をしたくないファイルを定義するファイル名。このファイルにバージョン管理をしたくないファイル名を書くと書いたファイルが表示されなくなる。
                        例）　パスワードなどの機密情報を載せたデータ。（バージョンを管理してしまって流出することがある。）
11/16 Javascript　基礎３
            Promise : 処理を並列して行えないJavascriptを並列処理できるようにする。

4/2 Udemy(JS)
            !　＋　tab : HTMLのheadで必要なコードを一括で書ける。
            let const : 変数。
            function : 関数。一連の手続きを実行する文の集まり。(名前がない関数を無名関数という。オブジェクト内の関数はこれが一般的。)
            オブジェクト　: 名前（プロパティー　or　キー）と値（バリュー）をペアで管理する入れ物。　{名前: 値}で書く。値には関数、オブジェクトも設定することができる。
                        オブジェクトのプロバティーに格納された関数をメソッドという。
            ドット記法　: obj.nameのような形式。
            ブラケット記法 : obj['name']のような形式。
            windowオブジェクト : Javascriptエンジンによって用意されているブラウザのグローバルオブジェクト。WebAPIsもその中の一つ。　検証ツールのコンソールで「window」と打つと色々見ることができる。
            JSエンジン　: 最近の主流はChromeでも使われるv8。これがあるとJSの実行環境を作ることができる。
            グローバルコンテキスト　: JSファイル直下のコード全て。
            関数コンテキスト : 関数内にあるコード全て。
            コールスタック : 実行中のコードが辿ってきたコンテキストの積み重ね。後入れ先だし（LIFO）
            ホイスティング : コンテキスト内で宣言した関数や変数の定義をコード実行前にメモリーに配置する。
            関数式 : const a = function () {}
